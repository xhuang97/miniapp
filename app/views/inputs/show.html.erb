<p id="notice"><%= notice %></p>

<table class="table">
  <thead>
    <tr>
      <th></th>
      <th>Datatime</th>
      <th>Type</th>
      <th>Sessionid</th>
      <th>Sessiondesc</th>
      <th>Sessioncond</th>
      <th>Isundo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @input._id %></td>
      <td><%= @input.dataTime %></td>
      <td><%= @input.type %></td>
      <td><%= @input.sessionId %></td>
      <td><%= @input.sessionDesc %></td>
      <td><%= @input.sessionCond %></td>
      <td><%= @input.isUndo %></td>
    </tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_input_path(@input) %> |
<%= link_to 'Back', inputs_path %>
